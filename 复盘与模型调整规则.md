# 复盘与模型调整规则

更新时间：2026-05-27

## 目标

复盘不是给交易找借口，而是回答三个问题：

- 当时的判断是否符合模型？
- 盈亏来自模型能力、执行偏差，还是市场随机波动？
- 需不需要调整模型，还是只记录样本继续观察？

## 复盘节奏

- 每个盯盘任务完成后：追加一条简短记录。
- 每个交易日结束后：做日度复盘。
- 每周结束后：做周度模型复盘，决定是否调参。
- 重大回撤或单日收益异常时：立即做专项复盘。

## 日度复盘格式

记录文件建议写入 `records/YYYY-MM-DD-review.md`。

每次日度复盘必须包含：

- 日期和复盘时间。
- 当日账户收益率、排名变化、总资产、现金占比。
- 已成交订单、pending 订单、取消或未执行订单。
- 持仓盈亏 Top 3 和亏损 Top 3。
- 是否触发止损、止盈、超仓、熔断。
- 当日强势主线是否与持仓一致。
- 模型判断是否执行到位。
- 最大错误和最大正确点。
- 次日动作：加仓、减仓、观察、禁买、候选池。

## 单笔交易复盘

每笔交易至少回答：

- 买入或卖出时的模型评分是多少？
- 交易理由是否仍成立？
- 最大反方证据是否被低估？
- 入场价格是否追高？
- 是否按预设止损/止盈执行？
- 如果没有执行，是 API、时间窗口、判断犹豫，还是规则缺失？
- 这笔交易应该进入模型样本，还是标记为异常样本？

## 归因分类

每个盈亏结果只能归入一个主因，避免复盘糊成一锅粥。

- `模型正确执行正确`：信号有效，执行符合规则。
- `模型正确执行错误`：判断对，但买卖点、仓位、时间窗口错。
- `模型错误执行正确`：模型方向错，但风控或执行救了账户。
- `模型错误执行错误`：判断和执行都错，优先复盘。
- `随机波动`：无明显可学习模式，只记录不调参。
- `数据缺失`：行情、持仓、订单或市场数据不可用导致误判。

## 调参门槛

不能因为一笔交易或一天盈亏就改模型。

允许调参的情况：

- 同类错误连续出现 3 次以上。
- 同一因子在 10 笔以上样本中稳定失效。
- 策场排名目标变化导致仓位目标需要切换。
- 市场制度、API 字段、结算规则发生变化。
- 风控规则被证明太松，导致可避免的大回撤。

禁止调参的情况：

- 单笔交易亏损后情绪化降低所有仓位。
- 单笔交易大赚后提高该因子权重。
- 用盘后结果反推盘前本不存在的理由。
- 没有样本数量，只凭感觉新增高权重因子。

## 策场模型调整重点

策场冲榜模型每周检查：

- 满仓是否提高排名，还是只放大波动。
- 动量仓是否跑赢核心底仓。
- 单股 20% 上限是否限制收益，还是有效降低回撤。
- 8% 止损是否过宽或过窄。
- 15% 止盈是否太早卖飞强趋势。
- top-movers 跟随是否存在追高损耗。

可调整项：

- 排名状态仓位区间。
- 标的评分权重。
- 动量仓与核心仓比例。
- 止盈从固定 15% 改为移动止盈的触发条件。
- 收盘前是否允许满仓隔夜。

## 银狐 A股模型调整重点

银狐 A股模型每周检查：

- 情绪周期分数是否能提前识别退潮。
- 炸板率权重是否足够。
- T+1 锁仓风险是否被低估。
- 尾盘买入是否优于早盘追高。
- 三路策略中哪一路贡献收益，哪一路拖累。

可调整项：

- 情绪周期各指标权重。
- 涨停先锋启用阈值。
- 尾盘买入权重。
- 单日新仓上限。
- 熔断阈值和恢复条件。

## 版本调整记录格式

每次模型调整必须追加：

```markdown
## YYYY-MM-DD 模型调整

- 调整对象：策场冲榜模型 / 银狐 A股激进模型
- 版本变化：v2 -> v2.1
- 触发原因：
- 样本依据：
- 调整内容：
- 预期改善：
- 风险：
- 生效时间：
```

## 红线

- 不把 API Key、token、cookie、私密凭据写入复盘。
- 不重写历史交易理由。
- 不把随机行情解释成模型胜利。
- 不为了冲排名取消硬止损。
